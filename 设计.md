Hermes 端到端链路优化方案
一、现状分析：链路断点诊断
根据代码分析，当前链路存在以下断点：

阶段	现状	问题
PM 输入
直接进入 AgentEngine.chat_stream()
无需求澄清，LLM 靠猜测补全需求
澄清 Agent
❌ 缺失
需求模糊导致后续大量返工
方案设计
❌ 缺失
LLM 直接写代码，没有设计文档
模块定位
依赖 search_files 逐个扫描
无结构化索引，效率低
代码生成
chat_sync 阻塞循环，串行执行
无并行，无流式体验
Lint/单测
lint_and_test 有，但输出粗糙
无通过率统计、无分文件结果
提交 PR
git_commit_and_pr 有，但标题/分支生成简单
无关联 issue、无审查者
核心架构瓶颈
当前 AgentEngine.chat_stream() 循环:
while turn < max_turns:
    result = provider.chat_sync(messages, tools)  ← 每次都阻塞等待 LLM
    for tc in result.tool_calls:
        yield tool_call event
        result_str = registry.run_tool(...)       ← 串行执行
        yield tool_result event
chat_sync 阻塞：所有 LLM 调用都是同步的（ThreadPoolExecutor 包装），最终轮次的文本内容是累积后一次性 yield，不是真正的流式
工具串行：多个工具调用按顺序执行，即使它们之间无依赖
无阶段路由：所有输入走同一条 prompt，没有根据输入类型路由到专门的 Agent
系统提示固定：soul 配置 + 工具列表，缺少针对 Conduit 实仓的专项上下文
Lint 结果无结构化：lint_and_test 返回的 stdout/stderr 是原始文本，通过率需要解析
二、目标架构设计
PM 自然语言输入
        │
        ▼
┌─────────────────┐
│  澄清 Agent     │  ← 新增：检测需求模糊度，问 3 个关键问题
│ (ClarifyAgent)  │     自动路由：简单请求直通主引擎
└────────┬────────┘
         │ 需求澄清完毕
         ▼
┌─────────────────┐
│  方案设计 Agent  │  ← 新增：生成 CHANGE_DESIGN.md
│ (PlanAgent)     │     模块定位 → 依赖分析 → 变更范围
└────────┬────────┘
         │ 方案确认
         ▼
┌─────────────────┐
│  主任务引擎     │  ← 改进：流式 + 并行工具执行
│ (AgentEngine)   │     • 流式输出 thinking + text_chunk
│                 │     • 独立工具并行（codemap scan / list_dir）
└────────┬────────┘
         │ 代码变更完成
         ▼
┌─────────────────┐
│  Lint/Test 引擎 │  ← 改进：结构化结果 + 通过率
│ (StructuredRun) │     • 逐文件通过率
│                 │     • 失败重试机制
└────────┬────────┘
         │ Lint/测试通过
         ▼
┌─────────────────┐
│  PR 生成引擎   │  ← 改进：智能分支/标题/Body
│ (PRGenerator)   │     • 关联 issue
│                 │     • 自动选择 reviewer
│                 │     • CI 状态轮询
└────────┬────────┘
         │
         ▼
      PR 链接
三、具体实现方案（8 个模块）
模块 1：澄清 Agent（ClarifyAgent）
问题：当前没有需求澄清机制，模糊需求直接进入主引擎。

方案：在 AgentEngine 前增加一层路由：

backend/runcore/agents/clarify.py  (新文件)
class ClarifyAgent:
    """Routes PM input: short-circuit simple requests, clarify complex ones."""
    # 需求复杂度阈值
    COMPLEXITY_PATTERNS = {
        'complex': [
            r'(add|implement|create|build).*(feature|module|api|system|auth|payment)',
            r'refactor.*(entire|large|core|main)',
            r'migrate.*(database|framework|language)',
            r'design.*(architecture|schema|protocol)',
        ],
        'simple': [
            r'fix.*bug',
            r'change.*text.*on.*button',
            r'update.*readme',
            r'add.*comment',
            r'typo.*in',
        ]
    }
    def is_complex(self, user_message: str) -> bool:
        # 匹配复杂模式 → 需要澄清
        # 匹配简单模式 → 直通主引擎
        # 都不匹配 → 默认澄清
澄清问题策略：

检测到缺少验收标准 → 问"如何验证这个功能？"
检测到涉及多个模块 → 问"哪个模块是变更的核心？"
检测到缺少 API 规范 → 问"这个 API 的输入输出格式是什么？"
检测到缺少数据模型 → 问"需要存储哪些字段？"
API 端点：

POST /api/clarify — 接收 PM 输入，返回澄清问题或直通结论
POST /api/clarify/resolve — 接收用户回答，继续流程
模块 2：方案设计 Agent（PlanAgent）
问题：LLM 直接写代码，没有设计阶段。

方案：新增 PlanAgent，在代码编写前生成结构化方案：

backend/runcore/agents/plan.py  (新文件)
方案文档格式（CHANGE_DESIGN.md）：

# Conduit 变更设计
## 需求摘要
[一句话描述]
## 涉及模块
- frontend/src/api/  (影响)
- backend/models/   (影响)
## 变更文件清单
| 文件 | 操作 | 原因 |
|------|------|------|
| src/api/user.ts | 修改 | 添加新字段 |
## API 变更（如有）
### GET /api/users/:id
新增响应字段: `subscription_tier`
## 数据模型变更（如有）
### User model
新增字段: `subscription_tier: enum(free, pro, enterprise)`
## 测试策略
- 单元测试: auth.service.spec.ts
- E2E 测试: user-profile.spec.ts
## 风险评估
- 风险: 涉及数据库迁移
- 回滚方案: revert migration
工作流程：

PlanAgent 扫描代码库（使用 codemap）
分析影响范围
生成 CHANGE_DESIGN.md
用户确认后进入代码生成阶段
模块 3：流式主引擎（Streaming AgentEngine）
问题 1：chat_sync 是同步阻塞的，导致流式体验差。 问题 2：工具串行执行，无法并行。

方案：重构 AgentEngine.chat_stream()

async def chat_stream(self, user_message: str, ...):
    # Round 1-N: 并行工具执行
    while turn < max_turns:
        # 异步流式 LLM 调用（不阻塞）
        result = await self._llm_stream_round(messages)
        # 并行执行独立工具
        independent_tcs = self._group_independent_tools(result.tool_calls)
        tool_results = await asyncio.gather(*[
            self._run_tool_async(tc) for tc in independent_tcs
        ])
        # 依赖工具按序执行
        for tc in dependent_tcs:
            result_str = self.registry.run_tool(...)
            # ...
        # 非流式最终轮：直接输出
        if not result.tool_calls:
            yield json.dumps({'event': 'done'}) + '\n'
            return
新增 SSE 事件类型：

事件	用途
phase_start
阶段开始（clarify/plan/code/lint/pr）
phase_end
阶段结束
parallel_tools
工具并行执行组
lint_result
结构化 lint/test 结果
progress
进度百分比
关键改动：

OpenAIProvider.chat() 已经是异步的，但 engine 没有使用它
新增 await self.provider.chat() 替代 chat_sync
工具分组：按 read_file/list_dir 并行，汇总后再执行 write_file
模块 4：模块定位引擎（ModuleLocator）
问题：当前用 search_files 逐个目录扫描，效率低。

方案：增强 codemap/scanner.py → 集成到工具系统

backend/runcore/tools/module_locator.py  (新文件)
新增工具：

@register_tool(
    name='locate_module',
    description='Locate relevant code modules for a feature or bug',
    parameters={
        'type': 'object',
        'properties': {
            'query': {'type': 'string', 'description': 'Feature description or bug keywords'},
            'repo_name': {'type': 'string', 'description': 'Repository name'},
        },
        'required': ['query']
    }
)
def locate_module(query: str, repo_name: str, username: str = '') -> dict:
    """Use codemap to quickly locate relevant modules."""
    repo_path = _get_repo_path(username, repo_name)
    scan_result = scan_directory(repo_path, max_files=2000)
    # 语义匹配：提取关键词 → 过滤文件
    keywords = extract_keywords(query)
    scored_files = []
    for f in scan_result['files']:
        score = sum(1 for kw in keywords if kw.lower() in f['path'].lower())
        if score > 0:
            scored_files.append((score, f))
    scored_files.sort(key=lambda x: x[0], reverse=True)
    return {
        'success': True,
        'top_modules': [f for _, f in scored_files[:10]],
        'scan_stats': {'total_files': len(scan_result['files'])}
    }
模块 5：结构化 Lint/Test 引擎（StructuredLintEngine）
问题：lint_and_test 只返回 npm 命令的 stdout/stderr，需要手动解析。

方案：重构 lint_test_skill/tool.py

@dataclass
class TestResult:
    file: str
    passed: bool
    duration_ms: float
    errors: List[str]
    warnings: List[str]
    coverage: Optional[float] = None
@dataclass
class LintResult:
    file: str
    passed: bool
    errors: List[LintError]
    warnings: List[LintWarning]
@dataclass
class PipelineResult:
    lint_results: List[LintResult]
    test_results: List[TestResult]
    install_ok: bool
    @property
    def lint_pass_rate(self) -> float:
        if not self.lint_results: return 1.0
        return sum(1 for r in self.lint_results if r.passed) / len(self.lint_results)
    @property
    def test_pass_rate(self) -> float:
        if not self.test_results: return 1.0
        return sum(1 for r in self.test_results if r.passed) / len(self.test_results)
    @property
    def overall_pass(self) -> bool:
        return self.install_ok and self.lint_pass_rate >= 0.95 and self.test_pass_rate >= 0.8
解析器（支持主流测试框架）：

def _parse_vitest_output(stdout: str) -> List[TestResult]:
    """Parse vitest JSON output to structured results."""
    try:
        # 尝试 JSON 输出模式
        data = json.loads(stdout)
        return [_parse_vitest_json(data)]
    except json.JSONDecodeError:
        # 回退到文本解析
        return _parse_vitest_text(stdout)
def _parse_eslint_output(stdout: str) -> List[LintResult]:
    """Parse ESLint JSON output to structured results."""
    data = json.loads(stdout)
    # ... 映射到 LintResult
改进点：

运行前先 npm run lint -- --format json 获取结构化输出
运行 vitest --reporter=json 获取逐文件结果
覆盖率：vitest --coverage --coverage.reporter=json
失败重试：对单个失败文件重跑 1 次
生成报告摘要
模块 6：PR 生成引擎（PRGenerator）
问题：当前 git_commit_and_pr 生成的 PR 标题简单，无 issue 关联。

方案：增强 pr_skill/tool.py

@register_tool(
    name='generate_pr',
    description='Generate a pull request with smart title, description, and metadata',
    parameters={
        'type': 'object',
        'properties': {
            'repo_name': {'type': 'string'},
            'issue_number': {'type': 'integer', 'description': 'Link to existing GitHub issue'},
            'reviewers': {'type': 'array', 'items': {'type': 'string'}, 'description': 'GitHub usernames'},
            'labels': {'type': 'array', 'items': {'type': 'string'}},
            'design_doc': {'type': 'string', 'description': 'Path to CHANGE_DESIGN.md'},
            'lint_result': {'type': 'object', 'description': 'Structured lint/test result'},
            'draft': {'type': 'boolean', 'description': 'Create as draft PR'},
        },
        'required': ['repo_name']
    }
)
def generate_pr(
    repo_name: str,
    issue_number: int = None,
    reviewers: List[str] = None,
    labels: List[str] = None,
    design_doc: str = None,
    lint_result: dict = None,
    draft: bool = False,
    username: str = ''
) -> dict:
    """Generate a high-quality PR with structured metadata."""
    # 1. 分析变更类型（feat/fix/docs/refactor）
    change_type = _detect_change_type(changed_files)
    # 2. 生成智能标题
    title = _generate_pr_title(change_type, changed_files, issue_number)
    # 3. 生成结构化 Body
    body = _generate_pr_body(
        title=title,
        design_doc=design_doc,
        lint_result=lint_result,
        issue_number=issue_number,
        changed_files=changed_files,
    )
    # 4. 创建分支（基于 issue 或变更类型）
    branch = f'{change_type}/{issue_number or _short_hash(changed_files)}'
    # 5. Git 操作
    _git_add_commit_push(repo_path, branch, title)
    # 6. 创建 PR（关联 issue + 添加 reviewer + 添加 labels）
    pr_url = _create_pr_with_metadata(
        title=title,
        body=body,
        branch=branch,
        base=base_branch,
        issue_number=issue_number,
        reviewers=reviewers,
        labels=labels or [change_type],
        draft=draft,
    )
    return {
        'success': True,
        'pr_url': pr_url,
        'branch': branch,
        'change_type': change_type,
        'lint_passed': lint_result.get('overall_pass') if lint_result else None,
        'linked_issue': issue_number,
    }
PR Body 模板：

## Summary
<!-- auto-generated from LLM -->
## Motivation
<!-- link to issue #X -->
## Changes
<!-- table of changed files -->
## Test Results
| Category | Pass Rate | Status |
|----------|-----------|--------|
| Lint | 98% | ✅ |
| Unit Tests | 12/12 | ✅ |
| Coverage | 85% | ✅ |
## Checklist
- [ ] Tests added/updated
- [ ] Documentation updated
- [ ] No breaking changes
---
_Generated by Hermes AI_
模块 7：前端 Pipeline 状态 UI（PipelineStatus）
问题：当前前端只有消息气泡，无 Pipeline 进度感知。

方案：新增 Pipeline 状态面板

electron/src/renderer/components/PipelineStatus.tsx  (新文件)
UI 设计：

┌─ Pipeline Progress ─────────────────────────┐
│ [1. Clarify] → [2. Plan] → [3. Code] → [4. Lint] → [5. PR] │
│   ✓ Done        ● Running    ○ Pending    ○ Pending    ○ Pending │
│                                                      │
│   ┌─ Current: Code Generation ─────────────────────┐ │
│   │ 🔍 Locating modules...                        │ │
│   │ 📝 Writing src/api/user.ts                   │ │
│   │   └─ src/api/user.ts ✓                       │ │
│   │ 📝 Writing src/models/User.ts                │ │
│   │   └─ src/models/User.ts ✓                    │ │
│   └─────────────────────────────────────────────┘ │
│                                                      │
│   ┌─ Lint Preview ─────────────────────────────────┐ │
│   │ ████████████████████░░░░░ 78%                 │ │
│   │ ESLint: 42/43 passed (97%)                    │ │
│   │ Vitest: 11/12 passed (91%)                    │ │
│   └─────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────┘
SSE 事件消费：

// useSSE.ts 新增处理
if (evType === 'phase_start') {
  pipelineStore.setPhase(event.phase)
  pipelineStore.setStatus('running')
}
if (evType === 'phase_end') {
  pipelineStore.completePhase(event.phase)
}
if (evType === 'lint_result') {
  pipelineStore.updateLint(event.result) // 结构化结果
  pipelineStore.setProgress(event.percent)
}
if (evType === 'parallel_tools') {
  pipelineStore.setParallelTools(event.tools)
}
模块 8：前端设置页面增强（SettingsPage）
新增设置项：

设置项	类型	说明
pipeline.autoClarify
boolean
复杂需求自动进入澄清阶段
pipeline.requirePlanApproval
boolean
代码编写前需要确认方案
pipeline.lintThreshold
number (0-1)
Lint 通过率阈值，默认 0.95
pipeline.testThreshold
number (0-1)
测试通过率阈值，默认 0.80
pipeline.autoPR
boolean
Lint 通过后自动创建 PR
pipeline.defaultReviewers
string[]
默认 PR reviewer 列表
codemap.enabled
boolean
启用代码结构索引加速定位
四、实现优先级与排期
阶段 1（MVP - 核心链路跑通）：预计 2-3 天
  ├─ P0: 流式 AgentEngine 重构（模块 3）
  ├─ P0: 结构化 Lint/Test 引擎（模块 5）
  ├─ P0: PR 生成引擎增强（模块 6）
  └─ P1: Pipeline 状态 UI（模块 7）
阶段 2（增强 - 需求质量）：预计 2 天
  ├─ P0: 澄清 Agent（模块 1）
  ├─ P0: 方案设计 Agent（模块 2）
  └─ P1: 模块定位引擎（模块 4）
阶段 3（完善 - UX 打磨）：预计 1-2 天
  ├─ P1: 前端设置增强（模块 8）
  └─ P2: 并行工具执行优化
五、关键风险与缓解
风险	缓解方案
流式 LLM + 并行工具 → 上下文混乱
工具结果异步汇入消息历史，确保顺序
结构化 lint 解析失败
保留原始 stdout fallback，解析失败不影响主流程
澄清问题过多 → 用户体验差
简单请求（< 3 个关键词）自动短路
PR 创建失败
生成完整的 git 命令，用户可手动执行
Windows 路径兼容
所有路径操作已使用 os.path 跨平台处理